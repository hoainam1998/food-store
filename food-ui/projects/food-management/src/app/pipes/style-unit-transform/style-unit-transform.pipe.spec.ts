import { StyleUnitTransformPipe } from './style-unit-transform.pipe';

describe('StyleUnitTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new StyleUnitTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
