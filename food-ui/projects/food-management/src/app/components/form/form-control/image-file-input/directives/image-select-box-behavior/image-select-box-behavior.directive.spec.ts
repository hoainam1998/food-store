import { ImageSelectBoxBehaviorDirective } from './image-select-box-behavior.directive';

describe('ImageSelectBoxBehaviorDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageSelectBoxBehaviorDirective();
    expect(directive).toBeTruthy();
  });
});
