import { ImageFileInputBehaviorDirective } from './image-file-input-behavior.directive';

describe('ImageFileInputBehaviorDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageFileInputBehaviorDirective();
    expect(directive).toBeTruthy();
  });
});
